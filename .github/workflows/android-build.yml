name: Unit Test

on:
  pull_request:
    branches: ["main"]
    paths:
      - 'kotlin/**'

jobs:
  android-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: "gradle"

      - name: Grant execute permission for gradlew
        working-directory: kotlin
        run: chmod +x gradlew

      - name: Run Unit Test
        working-directory: kotlin
        run: ./gradlew jujubasvg:testAndroidHostTest
