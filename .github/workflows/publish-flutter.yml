name: Publish flutter to pub.dev

on:
  workflow_dispatch:
  push:
    tags:
      - 'jujubasvg-flutter-[0-9]+.[0-9]+.[0-9]+*'

defaults:
  run:
    working-directory: ./flutter

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
